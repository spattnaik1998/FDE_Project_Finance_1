[pytest]
testpaths = tests
pythonpath = src
addopts = -q --strict-markers
markers =
    live: hits a real model provider API; skipped without credentials
filterwarnings =
    ignore::DeprecationWarning
